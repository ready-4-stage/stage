spring:
    profiles: dev
server:
    port: 5000
    shutdown: graceful

management:
    health:
        probes:
            enabled: true

token:
    authenticationUrl: '/authenticate'
    publicUrl: '/v1'

jdbc: 'jdbc:h2:mem:'
